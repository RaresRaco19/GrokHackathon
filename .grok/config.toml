# Project MCP: kit rule file.
# Lab command: grok mcp add --scope project rules -- python3 python/rules_mcp.py
[mcp_servers.rules]
command = "python3"
args = ["python/rules_mcp.py"]
enabled = true

# Plugin pack (skill + hook + MCP). Enable after the folder is trusted.
[plugins]
enabled = ["fnol-desk"]
